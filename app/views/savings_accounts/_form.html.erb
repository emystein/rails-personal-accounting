<%= form_with(model: savings_account) do |form| %>
  <% if savings_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(savings_account.errors.count, "error") %> prohibited this savings_account from being saved:</h2>

      <ul>
        <% savings_account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :currency %>
    <%= form.text_field :currency %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
